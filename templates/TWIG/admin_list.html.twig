{% extends 'menu_admin.html.twig' %}

    <div class="col-sm-10">
        <h1 class="text-center">Liste des messages</h1>
        <h2 class="text-center">Modifier ou supprimer</h2>

        <div class="row">
            <div class="col-sm-12 table-responsive">
                <table class="table table-striped table-condesed">
                    <thead>
                    <tr>
                        <th>Titre</th> <th>Cr&#233;&#233; le</th> <th>Modifié le</th> <th>Action</th>
                    </tr>
                    </thead>
                    <tbody>

                    <!-- Boucle affichage -->
                    {% for ligne in req %}
                        <tr> <td> {{ ligne.titre }} </td><td> {{ ligne.date_creation }}
                            </td><td> {{ ligne.date_maj }} </td>
                        <td><div class="btn-group">
                                <a class="btn btn-primary btn-sm" href="{{ path('Framework/Controller/UpdateBilletController', {'id': ligne.id}) }}" <admin_change.php.php?id=' . $ligne['id'] . ' "-->>Modifier</a>
                                <a class="btn btn-danger btn-sm"  href="{{ path('Framework/Controller/DeleteBilletController', {'id': ligne.id}) }}" <!--../controleur/delete.php?id=' . $ligne['id'] . ' " -->>Supprimer</a>
                            </div>
                        </td> </tr> <br/>
                    {% endfor %}

                   <!-- $req->closeCursor();
                    ?> -->
                    <!-- Fin boucle -->


                    </tbody>
                </table>

            </div>
        </div>
    </div>
</body>
